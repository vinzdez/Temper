<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <ImageView
        android:id="@+id/level"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:contentDescription="chapter header"
        android:scaleType="fitCenter"
        android:src="@drawable/chapter" />

    <TextView
        android:id="@+id/level_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="5dp"
        android:fontFamily="@font/euclidcircularbregular"
        android:textColor="@color/user_color"
        android:textSize="18sp"
        android:textStyle="bold"
        tools:text="Find your tools" />

    <TextView
        android:id="@+id/level_description"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="5dp"
        android:fontFamily="@font/euclidcircularbregular"
        android:textAlignment="center"
        android:textColor="@color/level_description_color"
        android:textSize="18sp"
        tools:text="Collect the best ways for you to notice and manage anger" />

</LinearLayout>
